import { Idk2Directive } from './idk2.directive';

describe('Idk2Directive', () => {
  it('should create an instance', () => {
    const directive = new Idk2Directive();
    expect(directive).toBeTruthy();
  });
});
